# 실행 계획 (ROADMAP)

ACSCENT ERP / NEANDER ERP 의 앞으로 할 작업을 한 곳에 모은 문서.
작업이 끝나면 해당 항목을 `[x]` 로 바꾸고 결과를 한 줄 남긴다.

**최종 갱신**: 2026-08-14

---

## 배경 — 왜 이 작업들을 하는가

두 시스템은 대상이 완전히 다르다.

| 시스템 | 대상 | 성격 |
| --- | --- | --- |
| **ACSCENT ERP** | 매장 알바 직원 | 공개. 단 `/admin` 은 관리자 전용 |
| **NEANDER ERP** | 이사진·핵심 직원 | 본사 전용. 특정 권한자만 |

그런데 점검 결과 **두 시스템 모두 게이트를 우회하는 경로가 있었고, 방향이 정반대**였다.

- **ACSCENT** — UI 는 잠겨 있는데 DB 가 열려 있다. `/admin` 은 비밀번호로 막혀 있지만 Firestore 규칙이 매장 컬렉션 전체에 `write: if true` 라, 알바가 매장 페이지 콘솔에서 Firestore SDK 를 직접 호출하면 관리자 페이지를 안 거치고도 스케줄·상품을 수정·삭제할 수 있다. **로그인 화면을 통과할 필요가 없다.**
- **NEANDER** — DB 는 잠겨 있는데 정적 콘텐츠가 열려 있었다. Firestore 규칙은 제대로 동작하지만, 코드에 하드코딩된 회의 발표덱은 규칙의 보호를 못 받아 **로그인 없이 전문이 읽혔다.**

즉 **UI 게이트와 DB 게이트가 서로 다른 기준을 쓰고 있던 것**이 근본 원인이다. 아래 작업들은 이 둘을 하나의 기준으로 모으는 과정이다.

---

## Phase 1 — 두 시스템 배포 분리 ✅ 완료

브랜치 `feat/split-acscent-neander`.

- [x] `next.config.mjs` — `APP_TARGET` 으로 `pageExtensions` 분기, 상대 시스템 라우트를 빌드에서 제외
- [x] NEANDER 라우트 17개 개명 (`page.tsx` → `page.neander.tsx`)
- [x] `middleware.ts` — 타깃별 게이트 (ACSCENT: `/admin` 보호 / NEANDER: `/neander` 외 리다이렉트)
- [x] `package.json` — `dev:neander` · `build:neander` · `start:neander`
- [x] `README.md` — 분리 방식·주의사항 명시

**검증 결과**

```
매장 도메인:  /neander/*                → 404
             이전 유출 청크 URL 직접 요청 → 404
             매장 번들에서 회의 브리핑 본문 검색 → 0건
본사 도메인:  / /id /admin              → 307 → /neander
```

라우트 36개 → 19개. 로그인 게이트로 가리는 게 아니라 **코드 자체가 없다.**

### 남은 수동 작업

- [ ] **Vercel 프로젝트 2번째 생성** — 같은 레포, 환경변수 `APP_TARGET=neander` 만 추가. 기존 프로젝트는 기본값이 `acscent` 라 변경 불필요
- [ ] 본사 도메인 연결

---

## Phase 2 — ACSCENT Firestore 조이기

> **목표**: `/admin` 게이트를 UI 잠금이 아니라 실제 경계로 만든다.
> **결정 사항**: 공유 비밀번호(`ADMIN_PASSWORD`) 방식은 유지하고, DB 쓰기만 서버 경유로 바꾼다.

유리한 조건이 두 가지 있다.

1. 매장 쓰기가 전부 `lib/db.ts` 한 곳을 지난다 (쓰기 함수 19개)
2. **관리자 컴포넌트 7개는 Firestore 를 직접 호출하는 곳이 0건** → `lib/db.ts` 만 바꾸면 화면 코드는 무변경

Admin SDK 서비스 계정 키는 이미 있다 — `neander-erp-mcp-team/secrets/acscentmanager-adminsdk.json` (같은 프로젝트 `acscentmanager`).

- [ ] 서비스 계정 키를 환경변수로 주입 (레포에 커밋 금지, Vercel 환경변수)
- [ ] 서버 API 라우트 신설 — `admin_session` 쿠키 검증 후 Admin SDK 로 쓰기
- [ ] `lib/db.ts` 쓰기 함수 19개를 fetch 경유로 전환
- [ ] `firestore.rules` — 매장 컬렉션 `read: true / write: false`
- [ ] **예외 유지**: 알바가 직접 써야 하는 `taskChecks`(업무 체크)와 건의함은 클라이언트 쓰기 허용
- [ ] 관리자 페이지 전 기능 회귀 테스트 (스케줄·업무·이벤트·상품·프로모션·향료·청소·건의함)

### 알려진 한계 (이번 결정의 트레이드오프)

공유 비밀번호를 유지하므로 **개인 식별과 감사 추적은 여전히 불가**하다. 누가 무엇을 바꿨는지 남지 않고, 비밀번호가 유출되면 회수 방법이 변경뿐이다. 나중에 필요해지면 Google 로그인 + 역할 기반으로 전환할 수 있다 (Phase 3 의 역할 체계를 그대로 재사용 가능).

---

## Phase 3 — NEANDER 권한 세분화

현재 `lib/neander/types.ts` 의 `Member.role` 은 **자유 문자열 표시용**이고 권한 판정에 쓰이지 않는다. 즉 등록된 팀원 전원이 동일 권한이며, 이사진과 핵심 직원 사이에 차등이 없다.

- [ ] 역할 정의 — `neander_member` / `neander_admin`
- [ ] `lib/neander/access.ts` 를 역할 기반으로 확장 (현재는 이메일 허용목록만 판정)
- [ ] `firestore.rules` 에 동일 역할 기준 반영 → UI 게이트와 DB 게이트를 일치시킴
- [ ] 팀원 등록·삭제 등 관리 기능을 `neander_admin` 으로 제한

### 회의 발표덱 정적 유출 (Phase 1 로 완화, 미완결)

`/neander/meetings/prep/*` 는 Firestore 가 아니라 **코드에 하드코딩된 정적 페이지**라 규칙의 보호를 못 받는다.

Phase 1 로 매장 도메인에서는 사라졌지만, **본사 도메인은 공개 URL 이라 주소를 아는 사람은 여전히 로그인 없이 읽을 수 있다.** 노출 범위가 "알바 전원"에서 "URL 을 아는 사람"으로 줄었을 뿐 0 이 아니다.

- [ ] 덱 콘텐츠를 `neander_meetings` 로 이관 → 기존 규칙이 그대로 보호
- [ ] 발표덱 엔진을 하드코딩이 아닌 데이터 기반으로 전환

---

## 백로그 (급하지 않음)

- [ ] **의존성 취약점** — 프로덕션 기준 15건 (critical 1: `websocket-driver`, high: `undici`·`nanoid`·`postcss`). `npm audit fix` 로 breaking 없이 대부분 해결. 단 `undici` 는 `package.json` overrides 에 `6.26.0` 으로 직접 핀되어 있는데 그 버전이 취약 → 값을 올려야 함. `next` 14.2.35 는 메이저 이동이 필요해 별도 판단
- [ ] **비대한 컴포넌트 분할** — `components/scheduler/WeeklySchedule.tsx` (1079줄), `app/neander/tasks/page.neander.tsx` (941줄), `components/admin/DailyTaskAdmin.tsx` (701줄). 해당 파일을 건드리는 작업 전에 선행하면 안전
- [ ] **MCP 서버 레포 위치** — `Desktop/smoat/neander-erp-mcp-team` 이 `smoat` 레포 안에 중첩되어 있다 (git log 에 smoat 커밋이 찍힘). 의도한 구조인지 확인 필요
- [ ] **`CLAUDE.md` 생성** — 반복 작업 시 구조 재파악 비용 절감

---

## 재무 모듈 (/neander/finance) — M1~M3 구현 완료, 실데이터 적재 대기

`neander-erp-m1`(별도 Supabase 프로젝트)로 만들려던 재무·장부 모듈을 **NEANDER ERP 안에
Firestore 기반으로 구현**했다. 설계안(`~/Downloads/neander-erp-m1/DESIGN.md`)의 화면·집계
정의는 그대로 따르되, 별도 DB·별도 배포·별도 로그인을 두지 않았다.

### 구현된 것

| 화면 | 경로 | 내용 |
| --- | --- | --- |
| 대시보드 | `/neander/finance` | 총수입·총지출·환급·순손익, 사업부손익표, 계정대분류×사업부 지출 매트릭스, 월별 추이, 사업장별, 상위 거래처, 구독비 |
| 거래 원장 | `…/ledger` | 7종 필터 + 검색, 페이지네이션, 행 클릭 수정, 엑셀 내보내기 |
| 검토 대기함 | `…/review` | 자동분류 미확정 건 승인. ↑↓ 이동 · Enter 확정 · E 상세 · 일괄 확정 |
| 엑셀 임포트 | `…/import` | 드래그앤드롭 → 헤더 자동탐지 → 중복 검사 → 자동분류 → 미리보기 → 적재. 배치 되돌리기 |
| 마스터 | `…/master` | 계정 317 · 계좌카드 27 · 자동분류 규칙 CRUD, 엑셀 기준 재적재 |

- 마스터는 실제 장부에서 추출해 `lib/neander/finance/master-data.ts` 에 넣어뒀다 (자동 생성 — 손으로 고치지 말 것)
- 자동분류는 ① 과거 이력(거래처별 95% 일치) ② 구독 키워드 규칙 ③ 계좌 기본값 순으로 시도하고, 확신 없으면 검토 대기함으로 넘긴다

### 검증 — 엑셀과 원 단위 일치 확인됨

```
npm run finance:verify            # 기본: 2607 장부
npm run finance:verify -- <경로>   # 다른 파일
```

2026-07 실측 결과 **565건 · 오류 0건**, 총수입 41,656,602 / 총지출 58,896,728 /
환급 143,700 / 순손익 △17,096,426 **전부 일치**. 사업부별 9개 행과 지출 매트릭스 합계도 일치.

### 아키텍처 — 재무만 서버 API 를 거친다 ⚠️

다른 NEANDER 모듈은 브라우저가 Firestore 에 직접 붙지만, **재무만 서버를 경유**한다.

```
브라우저 → /api/neander/finance/{data,mutate} → Admin SDK → Firestore
```

이유는 두 가지다.

1. **규칙을 게시할 수 없다.** `neander_fin_*` 보안 규칙을 배포하려면 Firebase 프로젝트
   (`acscentmanager`) 소유자 권한이 필요한데 우리에게 없다. 소유자는 제3자다.
   Admin SDK(서비스 계정)는 보안 규칙을 **통째로 우회**하므로 게시 없이 동작한다.
2. **재무에는 이 구조가 더 맞다.** "재무는 특정인만"을 규칙으로 표현하려면 매번 규칙을
   다시 게시해야 하는데, 서버에서는 코드로 즉시 통제된다.

접근 통제의 실제 근거는 `firestore.rules` 가 아니라 **`lib/neander/finance/server/auth.ts`** 다.
클라이언트가 보낸 Google ID 토큰을 서버에서 검증해 이메일을 얻고, ①NEANDER 팀원인지
(`neander_member_emails`) ②재무 대상인지(`NEANDER_FINANCE_EMAILS`) 두 단계로 판정한다.

실시간 구독(onSnapshot)은 쓸 수 없으므로 쓰기 후 `refresh()` 로 다시 불러온다.

**필요한 환경변수** (로컬 `.env.local`, 배포는 Vercel 환경변수):

| 변수 | 내용 |
| --- | --- |
| `FIREBASE_SERVICE_ACCOUNT_B64` | 서비스 계정 JSON 을 base64 인코딩. **`NEXT_PUBLIC_` 금지** — 규칙을 우회하는 전체 권한이라 브라우저로 나가면 Firestore 전체가 열린다 |
| `NEANDER_FINANCE_EMAILS` | 재무 접근 허용 이메일(쉼표 구분). **비우면 NEANDER 팀원 전체**가 회사 재무 원장을 본다 — 채우는 것을 강력 권장 |

### 적재 현황 (2026-08-14 기준)

| 월 | 건수 | 수입 | 지출 | 순손익 | 사업구분 |
| --- | ---: | ---: | ---: | ---: | --- |
| 2026-04 | 5 | 1,112,794 | 165,268 | 947,526 | ❌ 없음 |
| 2026-05 | 408 | 21,486,896 | 63,425,962 | △41,939,066 | ❌ 없음 |
| 2026-06 | 482 | 30,256,857 | 58,508,395 | △28,251,538 | ❌ 없음 |
| 2026-07 | 565 | 41,656,602 | 58,896,728 | △17,096,426 | ✅ 있음 |
| **합계** | **1,460** | | | | |

- 4월 5건은 2605 파일 안에 4월 날짜로 들어있던 건이다. `21,486,896 + 1,112,794 = 22,599,690` 으로 2605 시트 합계와 정확히 일치한다(지출도 동일).
- 마스터: 계정 317 · 계좌카드 27 · 자동분류 규칙 16

### 남은 일

- [x] 마스터 적재
- [x] 장부 임포트 2605·2606·2607 (`npm run finance:import`)
- [x] `NEANDER_FINANCE_EMAILS` 설정 — 4명으로 제한됨
- [x] 개편 전 계정 경로 정규화 (`npm run finance:normalize`)
- [ ] **검토 대기함 32건 처리** — 아래 참고
- [ ] **5·6월 사업구분 매핑** — 아래 참고
- [ ] **Vercel 환경변수 등록** — `FIREBASE_SERVICE_ACCOUNT_B64` 없이 배포하면 재무만 500
- [ ] **페이히어(POS) 매출 자동 반영** — 페이히어 Open API 문의 답변 대기(2026-08-24경). 조사 결과·재개 절차는 `NEANDER_FINANCE_PAYHERE.md`
- [ ] **은행 입출금·법인카드 자동 수집** — 조사 완료(팝빌 계좌조회 권고, 카드는 카드사 수에 따라 EDI/바로빌). 결정 대기. `NEANDER_FINANCE_BANK_FEED.md`

### 개편 전 계정 경로 정규화 — 완료, 32건은 사람 몫

2026-08-10 카테고리 개편으로 계정 경로가 바뀌어서, 개편 전 장부(~2606)의
계정이 현행 마스터와 어긋났다. 어긋나면 ①엑셀 내보내기의 회계코드·부가세·
자산·지점이 빈칸이 되고 ②편집 모달의 계정 드롭다운이 현재 값을 못 찾고
③**월별 비교가 깨진다**(법인세가 5·6월엔 `재무비용`, 7월엔 `세금공과`).

`npm run finance:normalize` 가 **소분류(잎) 이름은 개편 후에도 그대로**라는
점을 이용해 상위 경로를 교정한다. 같은 거래유형 안에서 소분류가 마스터에
유일하게 존재할 때만 고치고, 후보가 여럿이거나 없으면 건드리지 않는다 —
재무에서 추측으로 분류를 바꾸는 것은 안 하느니만 못하다.

교정 결과 마스터 일치율: 2605 93%→98% · 2606 90%→95% · 2607 98%

교정하지 못한 46건 중 손익에 잡히는 **32건은 `needs_review` 로 표시**해
검토 대기함으로 보냈다. 사람이 정해야 하는 것들이다:

| 건수 | 경로 | 왜 자동으로 못 정하나 |
| ---: | --- | --- |
| 24 | 지출·운영비·일반운영비·**소모품비** | 마스터에는 `일반소모품비`/`생카소모품비` 가 매장별(아이디·와우·홍대공용) 중분류 아래에 있다. 어느 매장 것인지는 거래를 봐야 안다 |
| 3 | 지출·운영비·홍대공용운영비·**전기수도요금** | 마스터에 아예 없다 — 계정을 신설할지 다른 계정에 붙일지 결정 필요 |
| 5 | **환급** 계열 5종 | 마스터의 환급 계정 21개에 해당 잎이 없다 |

나머지 14건은 `카드대금결제` 유형이라 손익 비대상이다. 계정이 없어도 집계에
영향이 없어 그대로 뒀다.

### ⚠️ 5·6월은 사업부손익이 (미정) 으로 잡힌다

구버전 장부(28열)에는 **사업대분류/사업소분류 열 자체가 없다**. 2026-08-10 개편 때
신설된 축이라 그 이전 장부에는 존재하지 않는다. 그래서 4~6월 895건은 사업구분이
비어 있고, 사업부손익 표에서 전부 `(미정)` 으로 몰린다.

**총계·계정별 집계·거래 원장은 정상이다.** 사업부 축으로 쪼개는 화면만 영향을 받는다.

채우려면 두 가지 방법이 있다.

1. 계정 → 사업구분 규칙을 만들어 일괄 채운다 (예: 계정중분류 `와우운영비` → `B2C·와우`).
   7월 데이터 565건에 계정과 사업구분이 모두 있으므로, 거기서 매핑을 추출할 수 있다.
2. 검토 대기함에서 손으로 채운다 — 895건이라 현실적이지 않다.

1번을 하려면 7월 기준 매핑의 커버율을 먼저 실측해야 한다. 커버되지 않는 계정은
사람이 정해야 하므로, 자동으로 채운 건은 `needs_review` 로 두는 편이 안전하다.

### 구버전 장부 흡수 방식 (참고)

파서가 두 스키마를 모두 읽는다 (`lib/neander/finance/xlsx.ts`).

| 구버전(~2606) | 신버전(2607~) |
| --- | --- |
| 상위카테고리 / 하위구분 / 세부항목 | 계정대분류 / 계정중분류 / 계정소분류 |
| 세부항목비고 | 계정소분류비고 |
| 날짜 열에 **헤더 이름 없음** | 거래일시 |
| 거래유형 `내부이체` | 거래유형 `자금거래` |
| (없음) | 사업대분류 / 사업소분류 / 프로젝트코드 / 환급매칭ID |

날짜 열은 이름으로 못 찾으면 "헤더가 빈 열 중 첫 데이터가 날짜로 읽히는 열"을 쓴다.
`내부이체 → 자금거래` 는 둘 다 계좌 간 이동(손익 비대상)이라 뜻이 같아 흡수했다.
뜻이 다른 값은 흡수하지 않는다 — 재무에서 조용한 재해석은 금물이다.

### 알려진 한계

- **Firestore 는 관계형이 아니다.** 계정 조인·집계를 앱 코드에서 하므로 정합성은 코드가 지킨다. 거래가 연 수천 건 규모라 지금은 문제없지만, 수만 건이 되면 전체 구독 방식(`subscribeFinTransactions`)을 월 단위 쿼리로 바꿔야 한다.
- **중복 검사 키는 유일하지 않다.** 같은 날 같은 거래처에 같은 금액이 실제로 두 번 청구되는 일이 있어(2607 실측 4건), 임포트는 키 존재가 아니라 **건수**를 비교한다.
- 원본 `neander-erp-m1` 은 `~/Downloads` 에 그대로 있다. 이 구현으로 대체됐으므로 정리하거나 보관 위치를 옮기면 된다.
