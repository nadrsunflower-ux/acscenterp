# 재무 리포트 확장 — 지출상세·사업부손익·구독관리 연구

작성: 2026-08-23 · 상태: **조사 완료, 구현 전 제안**
관련: `NEANDER_FINANCE_BANK_FEED.md`(계좌·카드 자동 수집), `NEANDER_FINANCE_PAYHERE.md`(POS 매출)

검토 대상: `2607(주)네안데르_장부.xlsx`(7월 장부, 28개 시트) 중 **지출상세(현금흐름·발생주의)**,
**사업부손익 + B2C/B2B/공용**, **구독서비스 관리**. 수치 검증은 ERP(Firestore `neander_fin_transactions`)
의 2026-07 데이터 565건으로 했다.

---

## 1. 결론

1. **세 시트 모두 통합거래장의 집계 뷰다.** 새 입력 없이 지금 ERP 데이터로 그대로 만들 수 있다.
   사업부손익 상단·하단 표는 이미 대시보드에 있고(`businessUnitPL`, `expenseMatrix`), 빠진 것은
   **계정 3단 트리 뷰**, **현금흐름/발생주의 전환**, **사업소분류별 상세**, **구독 인벤토리**다.
2. 다만 두 가지는 데이터 모델을 보강해야 제대로 된다.
   - **결제수단 종류**(계좌/카드/현금) — 현금흐름 기준을 계산하려면 "카드 결제건"을 식별해야 한다.
     지금은 별칭의 `(신법)`/`(국법)` 패턴으로 추정할 수밖에 없다.
   - **구독 마스터** — 지금의 "거래처 키워드 16개"는 **급여 이체를 구독비로 잡는다**(아래 3.3).
     서비스·결제수단·주기·귀속을 가진 마스터로 바꿔야 한다.
3. 제안 구조: **재무 > 리포트** 탭 아래 ① 지출상세 ② 사업부 ③ 구독 세 화면 + 공통 리포트 엔진
   (계정 트리 × 사업구분 × 기간 × 기준) + **드릴다운**(숫자 클릭 → 원장이 그 필터로 열림).
   엑셀에서는 "이 숫자가 어디서 왔나"를 보려면 SUMIFS 를 뜯어야 했다. 이게 엑셀 대비 가장 큰 이득이다.
4. 선행 수정 1건: 거래처 규칙에서 사람 이름 키워드(`이동주`, `김제연`)를 빼거나 계정 조건을 붙일 것.
   7월 구독비가 **9,310,600원으로 표시되지만 실제 구독 계정 합계는 4,180,260원**이다.

---

## 2. 엑셀 장부 검토

### 2.1 시트 지도

| 성격 | 시트 | ERP 대응 |
|---|---|---|
| 입력(정본) | 통합거래장(29열) · 통합_MAP(계정 317) · 계좌카드목록 · 통합_LISTS · 드롭다운목록 | 원장 · 계정/계좌 마스터 ✅ |
| 집계 — 전사 | 핵심대시보드 · 통합대시보드 · 대시보드_{네안데르·안다르·일해라컴퍼니·와작홈즈} | 대시보드 일부 ✅ (사업장별 손익) |
| 집계 — 계정 축 | **지출상세(현금흐름)** · **지출상세(발생주의)** | ✕ |
| 집계 — 사업부 축 | **사업부손익** · **B2C** · **B2B** · **공용** · 프로젝트손익 | 손익표·매트릭스 ✅ / 상세 ✕ |
| 집계 — 주제 | **구독서비스 관리** · 예산vs결산 · USD환율 | 구독 키워드 집계만 △ |
| 조사·메모 | 장부자동화 조사보고 · 구독결제수단 정비 · OpenRouter배분 · 분류개편안 · 코드가이드 | 문서(`NEANDER_FINANCE_*.md`) |

⚠️ 다운로드된 7월 파일 사본은 **집계 시트에 수식·값이 없는 템플릿 상태**다(통합거래장만 데이터 1,120행,
그중 557행 공란). 각 시트의 뜻은 라벨·헤더 주석에서 복원했고, 숫자는 ERP 7월 데이터로 다시 계산했다.

### 2.2 지출상세(현금흐름) / 지출상세(발생주의)

**구조** — 통합_MAP 의 계정 3단 트리를 **전부** 행으로 펼친 표(362행). 매출 → 기타수입 → 투자·자본 →
마케팅비 → 영업비 → 제품개발운영비 → 서비스개발비 → 운영비 → 재무비용+세금공과 → 인건비 →
기획·전시비 → 기타비용 → 기타지출 → 자산투자비 순. 중분류마다 소계, 대분류마다 합계.

열: `수입금액 | 지출금액 | 지출금액(순수) | 차이(개인·환급) | 순금액(수입−순수)`
— "순수"는 개인사용(K=Y)·환급을 뺀 지출. "차이"는 그 차감액.

**두 시트의 차이** (헤더 주석 원문):

| 기준 | 규칙 | 뜻 |
|---|---|---|
| 현금흐름 | 카드대금결제 **포함**, 개별 카드사용내역 **제외** | 통장에서 돈이 나간 시점 |
| 발생주의 | 카드사용내역 **포함**, 카드대금결제 **제외** | 비용이 발생한 시점 (현재 ERP 기본) |

**7월 실측** (ERP):

| 항목 | 건수 | 금액 |
|---|---:|---:|
| 지출 — 카드 결제 | 240 | 18,170,593 |
| 지출 — 계좌·현금 | 92 | 40,726,135 |
| 카드대금결제 | 9 | 16,263,264 |
| **발생주의 지출** (카드 + 계좌) | 332 | **58,896,728** |
| **현금흐름 지출** (계좌 + 카드대금) | 101 | **56,989,399** |
| 차이 (청구 전 카드 사용분) | | 1,907,329 |
| 개인사용 | 1 | 12,800 |
| 환급 | 4 | 143,700 |

**ERP 현황** — 대시보드에 "계정대분류 × 사업부" 매트릭스만 있다. 계정 중·소분류까지 내려가는 트리가
없고, 기준 전환이 없다(항상 발생주의).

**적용안** — 리포트 탭 "지출상세":
- 계정 트리 테이블(대 ▸ 중 ▸ 소, 접기/펼치기). 0원 계정은 기본 숨김, "전체 계정 보기" 토글.
- 상단에 **기준 토글: 발생주의 | 현금흐름**. 현금흐름 = 지출 중 카드 결제건 제외 + 카드대금결제 포함.
- 열은 엑셀과 동일 5열 + 건수. 모든 숫자는 원장 드릴다운 링크.
- 필요한 모델 보강: `FinPaymentMethodMaster.kind: "account" | "card" | "cash"`. 마스터 31건 중
  카드 14장은 별칭 `(신법)`/`(국법)`, 현금은 `사무실지폐(9999)` 로 식별 가능하므로 seed 에서 채울 수 있다.

### 2.3 사업부손익 + B2C / B2B / 공용

**사업부손익** — 상단: 사업대분류×소분류 10행(수입·지출·환급·순손익). 하단: 계정대분류 × 사업부(10열)
지출 매트릭스. → **대시보드에 이미 구현됨.**

**B2C / B2B / 공용** — 사업소분류마다 한 섹션(와우·아이디·홍대공용·SMOAT·온라인 / 조향·개발·기타 / 공용)
× 전체 계정소분류 × `수입 | 지출 | 환급 | 순손익 | 비고`. B2C 시트만 1,462행인데 대부분 0행이다.
**비고 열이 핵심** — 사람이 쓴 설명이 들어 있다: "와우 매장 현장 결제 매출 — 하나·신한·KB·현대·BC·삼성
카드사 정산 105건", "전사 AI·클라우드 구독 — Claude, ChatGPT, 구글클라우드, Higgsfield 등 26건",
"조향 부문 전체 합계 — 매출 없이 원료·제작 선투자 발생". 숫자만으로는 안 읽히는 맥락이다.

**7월 실측 사업소분류 손익** (순손익 = 수입 − (지출 − 환급)):

| 사업부 | 건수 | 순손익 |
|---|---:|---:|
| B2C·와우 | 111 | 12,685,258 |
| B2C·아이디 | 60 | 9,239,080 |
| B2C·홍대공용 | 92 | △15,282,002 |
| B2C·온라인 | 25 | 383,315 |
| B2C·SMOAT | 31 | △1,350,248 |
| B2B·기타 | 6 | 15,653,850 |
| B2B·조향 | 29 | △5,816,215 |
| B2B·개발 | 3 | △1,895,155 |
| 공용·공용 | 167 | △30,714,309 |
| **합계** | 524 | **△17,096,426** |

관찰: 전체 적자 1,710만원의 원인은 **공용 △3,071만원**(인건비 2,429만원 대부분이 여기)과
**홍대공용 △1,528만원**이다. 둘 다 어느 사업부에도 배분되지 않는다. 와우·아이디가 흑자로 보이는 건
매장 공통비와 인건비가 빠져 있기 때문이다. "사업부가 실제로 돈을 버는가"는 **배분 후** 숫자로만 답할 수
있다(4장 2단계).

**적용안** — 리포트 탭 "사업부":
- 왼쪽 사업소분류 목록(10종 + 합계), 오른쪽 계정 트리 × 4열. 엑셀 B2C/B2B/공용 세 시트를 한 화면으로.
- 비고 열 대체: 계정 행마다 **자동 요약** — 상위 거래처 3개·건수·대표 비고를 거래에서 생성
  ("쿠팡 외 4곳 · 13건"). 사람이 쓴 설명은 `월 마감 메모`(3단계)로 따로 받는다.
- 배분 전/후 열 전환(2단계 이후).

### 2.4 구독서비스 관리

**엑셀** — 서비스 16개 ↔ 거래처 키워드, `건수 | 지출합계 | 환불/수입 | 순지출 | 비중`, 하단에
"분류안된 구독비"(계정소분류=구독서비스비인데 키워드 미매칭) 행. 집계 기준은 `운영비 > 일반운영비 > 구독서비스비`.
보조 시트 둘이 더 중요하다:
- **구독결제수단 정비** — 18개 서비스, 5~6월 582만원(월평균 291만원, 연 3,493만원). 결제수단 7개
  **전부 개인 명의 카드·계좌, 법인카드 0건**. 한 서비스가 카드 2~3장에 흩어짐(Google Cloud·Anthropic·
  Supabase·카페24). 권장: 카드 4장(공용SaaS / 인프라 / 마케팅 / 매장운영) = "카드 1장 = 목적 1개".
- **OpenRouter배분** — API 키별 사용량 비율로 결제액 1,160,151원을 SMOAT 75.4% / 사내개발 24.6% 로 배분.

**ERP 현황** — `neander_fin_vendor_rules` 16개 키워드 매칭 집계(`subscriptionSpend`)만 있다.

**7월 실측 — 규칙이 틀렸다**:

| 집계 방법 | 건수 | 금액 |
|---|---:|---:|
| 거래처 키워드 규칙 매칭 (현재 ERP 표시) | 45 | **9,310,600** |
| 그중 `이동주` 키워드 → "유튜브(이동주)" | 4 | 3,030,300 ← 급여 이체 |
| 그중 `김제연` 키워드 → "OpenRouter 대납" | 2 | 2,513,987 ← 급여 이체 |
| 계정소분류가 구독 계정(구독서비스비·툴구독비·개발프로그램구독비) | 40 | **4,180,260** |
| 구독 계정인데 규칙 미매칭 (카카오페이) | 2 | 138,000 |

월별 추이(규칙 기준) 5월 569만 → 6월 974만 → 7월 931만은 오매칭이 섞여 신뢰할 수 없다.
7월 구독 실제는 **약 418만원**이고, 결제수단은 여전히 개인카드 3513·7889·3800·1804·4306·4528 에 분산돼 있다.

**적용안** — 구독 마스터 + 리포트 탭 "구독":
```
neander_fin_subscriptions
  service        "Anthropic (Claude)"
  keywords       ["ANTHROPIC"]            // 거래처 매칭
  acctMinors     ["구독서비스비","툴구독비"] // 이 계정일 때만 매칭 (급여 오매칭 차단)
  last4          ["3513"]                 // 기대 결제수단 — 다르면 경고
  cycle          "monthly" | "usage"
  expected       150000                   // 월 예상액 — 초과 시 경고
  owner          memberId
  allocation     { bizMajor, bizMinor } | { rule: "usage", shares: {...} }
  status         "active" | "review" | "cancelled"
```
화면: 서비스 × 월 히트맵(최근 6개월), 이번 달 **미결제·중복결제·예상 초과** 배지, 결제수단 정비 진행률
(개인카드 비율), 미매칭 구독 목록(원장 링크). 구독결제수단 정비 시트의 "권장 카드" 열이 그대로 마스터가 된다.

---

## 3. 다른 시스템은 어떻게 하나

| 영역 | 대표 제품 | 방식 | 우리에게의 시사점 |
|---|---|---|---|
| 사업부 손익 | QuickBooks Online(Class/Location), Xero(Tracking Category) | 거래마다 분류 축 1~2개를 태그하고 P&L 을 그 축으로 **열 분할**. 리포트 설정에서 열 기준을 고른다 | 우리 사업대·소분류 = 이들의 tracking category 2단. 이미 같은 구조다. 빠진 건 "열 분할 P&L 화면" |
| 현금/발생 전환 | QuickBooks Online, Xero | 리포트 상단 **Accounting method: Cash / Accrual 토글** 하나. 데이터는 하나, 보는 기준만 바꿈 | 지출상세 두 시트를 두 화면이 아니라 **토글 하나**로 |
| 구독·SaaS 지출 | Ramp, Brex, Spendesk, Zluri, Airbase | 구독 **인벤토리**(금액·갱신일·담당·결제카드) + 중복·미사용 감지 + **카드 1장=용도 1개**(가상카드) | 엑셀 "구독결제수단 정비"와 같은 결론. 마스터화 + 경고가 핵심 |
| 공통비 배분 | D365 Finance, FinOps 관행 | 코스트풀(공용 인건비·임차료) × 드라이버(매출비율·인원·사용량) × 규칙. 드라이버를 바꿔 시뮬레이션 | 공용 △3,071만원을 배분해야 사업부 손익이 의미를 가짐. OpenRouter배분 시트가 이미 "사용량 드라이버" 사례 |
| 국내 SMB 경리 | 경리나라, 자비스, 더존 | 계좌·카드·세금계산서 **자동 수집**, 계정과목 자동 분류, 전 사업장 통합 손익 | 수집은 `BANK_FEED` 문서대로 외부 붙이기. 사업부 손익·구독 인벤토리는 이들에 약해 **자체 ERP 의 차별점** |

출처: [QuickBooks — Class/Location P&L](https://quickbooks.intuit.com/learn-support/en-us/help-article/class-list/get-started-class-tracking-quickbooks-online/L04INPWiy_US_en_US) ·
[QuickBooks — Cash/Accrual 리포트 기준](https://quickbooks.intuit.com/learn-support/en-us/help-article/product-preferences/choose-cash-accrual-accounting-methods-reports/L64yocn6B_US_en_US) ·
[Xero — P&L 과 tracking category](https://central.xero.com/0/article/Profit-and-Loss-New-US) ·
[Zluri — SaaS spend management 2026](https://www.zluri.com/blog/saas-spend-management-tools) ·
[Ramp vs Brex 2026](https://www.techrepublic.com/article/ramp-vs-brex/) ·
[D365 — 공유서비스 비용 배분](https://erpsoftwareblog.com/2025/07/allocating-shared-service-costs-across-business-units-with-d365-finance/) ·
[FinOps — shared cost allocation](https://finopsschool.com/blog/shared-cost-allocation/) ·
[경리나라](https://www.serp.co.kr/home/home_1000.html) · [자비스](https://jobisbiz.co/) ·
[국내 회계 프로그램 비교 2026](https://www.lido.app/kr/hoegye-program-bigyo)

---

## 4. 발전 로드맵

### 1단계 — 지금 데이터로 되는 것 (2~3주)

공통 **리포트 엔진** `lib/neander/finance/report.ts`:
- `accountTree(accounts)` — 통합_MAP 순서 그대로 대 ▸ 중 ▸ 소 트리.
- `aggregate(rows, { month, basis: "accrual"|"cash", scope: {bizMajor?, bizMinor?, site?} })`
  → 트리 각 노드에 `income / expense / expensePure / diff / net / count / txIds`.
- 셀 → 원장 드릴다운: 원장의 열 필터 모델(`sheetFilter.ts`)을 URL 쿼리로 직렬화해 링크.

화면 3개(재무 > 리포트): 지출상세(트리 + 기준 토글) · 사업부(소분류 선택 + 트리 + 자동 요약) ·
구독(키워드 기반 우선, 계정 제한으로 오매칭 차단).

검증 기준(7월): 총수입 41,656,602 / 발생주의 지출 58,896,728 / 현금흐름 지출 56,989,399 /
환급 143,700 / 순손익 △17,096,426 / 구독 4,180,260. **원 단위로 맞아야 한다.**

### 2단계 — 모델 보강 ✅ 완료 (2026-08-23)

- `FinPaymentMethodMaster.kind`(통장 12 · 카드 14 · 현금 1) 추가. `makeIsCard` 가 `kind` 를 우선 보고,
  없으면 `personal` 로 폴백 → 마스터 재적재 전에도 동작.
- `neander_fin_subscriptions` 마스터 22종. **키워드를 여러 개** 둘 수 있게 한 것이 실질 개선 —
  `CLAUDE.AI SUBSCRIPTION` 3건 483,175원은 키워드 하나(`ANTHROPIC`)로는 잡히지 않았다.
  7월 미매칭 2건 138,000 → **0건**.
  구독 화면에 경고 5종(카드 분산 · 결제 없음 · 급증 · 예상 초과 · 확인 필요) 추가.
  기준선은 마스터의 `expected` 가 아니라 **직전 달들의 중앙값** — 아무도 예상액을 안 채워도 울린다.
- **공통비 배분 규칙** `neander_fin_allocations` + 배분 엔진(`lib/neander/finance/allocation.ts`).
  드라이버 3종(매출비율 / 지출비율 / 고정비율), 규칙 간 연쇄를 위상 정렬로 처리(순환 시 경고),
  반올림 잔차는 최대 항목에 귀속. 사업부 리포트에 배분 전/후 토글 + 배분 내역 줄 표시.
  **초안 3개는 전부 비활성으로 시드** — 배분은 사실이 아니라 경영 판단이라 사람이 켜기 전에는
  아무것도 바뀌지 않는다.

  ⚠️ OpenRouter 사용량 배분은 규칙으로 넣지 않았다. 이미 장부에서 수동 재분류
  (2026-07-31 SMOAT △285,535 ↔ 공용 +285,535)돼 있어 규칙까지 걸면 이중 계상된다.

  2026-07 배분 시뮬레이션(규칙 2개 켠 경우): 4,680만원이 7줄로 이동, 총합 불변(△17,083,626),
  와우 12,685,258 → △5,309,442 / 아이디 9,239,080 → △6,641,666.
  매출비율의 한계로 그 달 매출이 0 인 조향·개발은 공통비를 받지 않는다 — 화면과 규칙 설명에 명시.

### 3단계 — 운영 (진행 중)

**예산 vs 결산** ✅ 완료 (2026-08-24)
- `neander_fin_budgets` — 문서 1개 = 한 달. `lines` 키는 계정 트리 경로 `대|중|소`.
  317개 계정의 이 경로가 전부 유일함을 확인했고(거래유형 교차 0건) 리포트 트리 노드와 같은 값이라
  집계와 바로 맞물린다.
- 리포트 › 예산 화면: 월 선택, 계정 트리 × `예산 | 결산 | 남은 예산 | 집행률`,
  **잎(계정소분류)에만 입력**(상위는 합계를 굴려 보여줌 — 둘 다 받으면 어긋날 때 어느 쪽이 맞는지
  알 수 없다), 원장 시트와 같은 초안 저장, **지난달 예산 복사**.
- 범위를 **지출 계정 12개 대분류로 한정**. 엑셀 시트도 그렇고, 매출 목표는 성격이 달라
  (달성률 vs 집행률) 섞으면 차액의 부호가 헷갈린다.
- 결산은 **순수 지출**(지출 − 개인사용 − 환급). 개인사용은 회수 대상이고 환급은 이미 돌려받은 돈이라
  예산을 소진한 금액이 아니다.
- 엑셀 매핑 부담 거의 없음: `예산vs결산` 세부 165개 중 **161개가 현행 계정소분류와 일치**.
  불일치 4개 = `소모품비`, `생카 소모품비`, `국민건강고용산재`, `비업무관련식대`.
  ⚠️ 시트의 예산 열은 `10월·11월·12월` 로만 적혀 연도가 모호하고 8월 이후가 비어 있어,
  임포트가 아니라 **화면 입력**으로 시작하는 것이 맞다.

**남은 것**
- **월 마감**: 월별 `closed` 상태 + 마감 메모(엑셀 비고 열의 역할) + 핵심대시보드 "데이터 품질 검증"
  (거래유형 공란·미분류·환급 미매칭·프로젝트 미태깅 건수)을 마감 체크리스트로.
- 건수 증가 대비 `data` 라우트 월 단위 조회.
- ⛔ **계좌·카드 자동 수집**(`BANK_FEED`) — 팝빌 계약(월 4만원)·카드 경로 결정 대기. 코드가 아니라
  계약이 먼저다.
- ⛔ **POS 매출**(`PAYHERE`) — 페이히어 Open API 답변 대기.

### 즉시 ✅ 완료

- 구독 집계를 `acctMinor ∈ 구독 계정`으로 제한(2026-08-23). 키워드를 지우지 않은 이유:
  `이동주`·`김제연` 은 **진짜 대납 구독비도 갖고 있었다**(유튜브 29,800 / OpenRouter 513,975).
  키워드를 지웠으면 543,775원을 잃었을 것이고, 계정 조건 방식이 급여 5,000,512원만 정확히 걸러냈다.
- 마스터 적재 완료(2026-08-24): 구독 22종 · 배분 규칙 3개(비활성) · 결제수단 `kind` 27/27.
  적재 규칙은 `lib/neander/finance/server/seed.ts` 한 곳에 있고 화면 버튼과
  `npm run finance:seed-master` 가 같은 함수를 쓴다.

---

## 5. 설계 메모 (구현 시 참고)

- 트리 집계는 클라이언트에서 한다. 1,460건×317계정은 즉시 계산된다. 월 단위 로드는 3단계 문제.
- 현금흐름 기준의 정의를 코드 한 곳(`report.ts`)에 두고 대시보드·지출상세·내보내기가 같은 함수를 쓴다.
  엑셀은 시트마다 SUMIFS 조건이 달라 기준이 갈라지는 게 문제였다.
- 엑셀 내보내기(`export.ts`)에 지출상세·사업부 시트를 추가하면 회계사무소 전달용 장부가 ERP 에서 나온다.
  현재는 통합거래장 1시트만 내보낸다.
- 리포트 숫자의 색: 음수 △ 표기 + rose. 사업부·계정 축 색은 `ui.tsx` SERIES 규칙을 따른다.
