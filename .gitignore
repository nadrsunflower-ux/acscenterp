# dependencies
/node_modules
/.pnp
.pnp.js

# testing
/coverage

# next.js
/.next/
/.next-neander/
/.next-*-check/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files
.env*.local
.env

# typescript
*.tsbuildinfo
next-env.d.ts

# vercel
.vercel

# 로컬 폰트 패키지(앱은 CDN 사용) — 커밋 제외
Pretendard-1.3.9/

# Firebase service account keys — NEVER commit
*adminsdk*.json
*service-account*.json
.claude/

# 규칙 게시용 로컬 CLI (scripts/deploy-rules.sh)
/.firebase-cli/

# UI 캡처 산출물 (e2e/capture.mjs)
/e2e/shots/

# Playwright 실패 시 남는 추적 산출물
/test-results/

# 인감 이미지 — 이 저장소는 public 이다.
# 법인인감은 그대로 문서 위조에 쓸 수 있는 재료라 깃에 올리지 않는다.
# 파일을 어디에 두는지는 public/images/seals/README.md 참고.
/public/images/seals/*.png
/public/images/seals/*.jpg
/public/images/seals/*.jpeg
