# 은행·카드·POS 엑셀 일괄 임포트 + AI 분류 — 구현 기록

작성: 2026-08-24 · 상태: **구현 완료, 실사용 검증 대기**
관련: `NEANDER_FINANCE_REPORTS.md`(리포트·예산), `NEANDER_FINANCE_BANK_FEED.md`(API 자동수집 — 보류),
`NEANDER_FINANCE_PAYHERE.md`(POS API — 보류)

목표: 은행·카드사·POS 에서 받은 엑셀을 **손으로 고치지 않고 그대로** 올리면 출처를 알아내고
중복을 걸러내고 자동분류하는 체계. 팝빌·페이히어 API 연동은 계약이 필요해 나중으로 미루고,
지금은 "엑셀을 받아서 올린다"를 제대로 만든다.

---

## 1. 검토한 실제 파일 8종

`/Users/idongju/Downloads/예시` 9개 파일(암호 3개 포함)을 열어 구조를 확인했다.

| 출처 | 확장자 | 헤더 행 | 계좌 정보 | 특이점 |
|---|---|---:|---|---|
| 국민 법인카드 (승인내역) | .xls | 1 | 카드번호 열 (3800·7889) | **해외 승인액이 달러** (`$8.85`) — 112건 중 34건 |
| 국민은행 | .xls | 7 | A1 요약줄 + 파일명 | 요약 6줄이 헤더 위에. `KB카드출금`+`국민카드` = 카드대금결제 |
| 신한은행 (grid) | .xlsx | 1 | **파일 안에 없음** | `적요`는 거래방식(BZ뱅크), `내용`이 실제 상대방. 적요 11종 중 `카드결` = 카드대금 |
| 신한 법인카드 | .xls | 1 | 이용카드 열 (6장) | 해외도 **원화 환산 제공** — 환율 불필요 |
| 토스뱅크 | .xlsx 🔒 | 9 (B열부터) | B3 + 파일명 | 금액에 부호 + `구분`(수입/지출). **상대 계좌번호를 준다** |
| 카카오뱅크 | .xlsx 🔒 | 11 (B열부터) | C5 | 금액은 양수, `구분`으로 방향 판단 |
| 페이히어 POS | .xlsx | 11 | — | 12행이 `전체합계` (넣으면 매출 2배). 총매출/실매출/환불 요약 별도 |
| 네이버 예약 | .xlsx 🔒 | 3 | — | 1~2행이 개인정보 경고문. 이용일시가 `26. 6. 1.(월) 오후 4:00` 꼴 |

🔒 = 암호 걸림 (토스 `000102` · 카카오 `000509` · 네이버 `sajindosa919`).

---

## 2. 구현

### 2.1 어댑터 체계 `lib/neander/finance/adapters/`

출처마다 파서를 두고 결과는 늘 같은 `ImportRow` 로 낸다. 그래서 미리보기·중복검사·자동분류·
적재는 출처를 모른다.

```
types.ts   ImportRow · AdapterResult · SourceAdapter 인터페이스
util.ts    헤더 찾기 · 날짜/금액 파싱 · 뒷4자리 추출 · 카드대금/자금거래 판정
cards.ts   국민 법인카드 · 신한 법인카드
banks.ts   국민 · 신한 · 토스 · 카카오
pos.ts     페이히어 (대사 전용)
naver.ts   네이버 예약 (대사 전용 · 개인정보 미저장)
ledger.ts  통합거래장 (기존 xlsx.ts 위임)
index.ts   레지스트리 · 자동 감지 · 암호 감지
```

설계 원칙 세 가지를 코드에 박아 뒀다:
1. **추측을 확정으로 올리지 않는다** — 어댑터가 아는 것은 `hint` 로 넘기고 최종 판단은 `classify.ts`.
2. **읽지 못한 행은 버리지 않고 이유와 함께 남긴다** — 재무에서 조용한 누락이 가장 나쁘다.
3. **원본을 보존한다** — 외화 원문·적요·잔액을 `note` 에 남겨 나중에 대조할 수 있게.

### 2.2 화면 (`/neander/finance/import`)

- 파일 **여러 개** 드래그. 한 달 마감이면 계좌 3개 + 카드 2개를 함께 올리는 게 자연스럽다.
- 파일마다: 감지된 출처(배지) · 출처 직접 지정 · 계좌 선택 · 환율 입력 · 오류/경고
- 암호 파일은 비밀번호를 물어보고 서버에서 풀어 온다 (`/api/neander/finance/decrypt`).
  **파일도 비밀번호도 서버에 남지 않는다** — 요청 안에서 풀어 응답으로 흘려보내고 끝.
- 적재 배치는 **파일 단위**. 되돌리기가 "그 파일만" 이어야 쓸모가 있다.
- 적재 전 12건을 실제로 보여준다. 숫자만 보고 적재하게 만들면 안 된다.

### 2.3 네이버 예약 — 대사 전용 + **개인정보 미저장**

두 가지 이유로 거래를 만들지 않는다.

**① 이중 계상** — 네이버 예약 결제는 `Npay정산` 입금으로 통장에 들어오고 그 입금이 이미 매출로
잡혀 있다. 실측으로 확인한 결과 **`Npay정산` 은 `온라인판매` 가 아니라 `아이디판매` 로 분류돼
있었다** (2026-05~07, 51건 2,388만원). 상품도 `[뿌리는 덕질] AI 이미지 분석 퍼퓸` 같은 매장 체험
상품이라 온라인이 아니다. 처음엔 온라인판매로 추정했다가 대사 차이가 △84% 로 나와서 잡았고,
아이디판매로 고치니 +34% 가 됐다(장부에 KB 카드정산·아현매 등 다른 채널이 섞여 있어 장부가 더 크다).

→ 기본 계정은 아이디판매로 두되, 화면에서 **맞춰볼 계정을 고를 수 있게** 했다. 같은 기간 각 매출
계정의 건수·금액을 함께 보여주므로 어느 쪽이 맞는지 눈으로 확인된다.

**② 개인정보** — 이 파일에는 예약자 124명의 이름·전화번호·이메일과 직원메모가 들어 있다. 파일
첫 두 줄에 네이버가 붙인 경고가 그대로 있다: *"이용목적이 달성되면 지체없이 파기해야 하며,
사업자가 이러한 주의 의무 위반 시 개인정보 관련 법령에 의해 처벌을 받을 수 있습니다."*

재무 집계에 이름·연락처는 **필요하지 않다.** 그래서 파서가 그 열을 **아예 읽지 않는다** — 읽지
않으면 Firestore 에도, AI 프롬프트에도, 화면에도 들어갈 길이 없다. 읽는 것은 이용일시·상품·
실결제금액·결제수단·결제상태·환불금액뿐이다.

검증(실제 파일): 원본의 예약자 124명·전화번호 123개·직원메모 2개가 결과 JSON 에 **0건** 포함.
이메일 도메인도 0건. 매출은 정상 집계(129건 4,196,200원).

부수적으로 처리한 것: 실결제 0원 3건(무료 증정 `special gift`)은 매출에서 빼되 건수를 밝힌다.
`오후 16:09` 처럼 24시간 표기에 오후가 붙어 오는 행이 있어 12를 더하면 28시가 되므로,
이미 12를 넘으면 그대로 둔다.

### 2.4 발견하고 고친 것들

**① 직원 이름을 자금거래로 오판정** (심각)
처음엔 계좌 별칭(`(신법)이동주`)을 근거로 자금거래를 판정했다. 그 결과 **토스 42건 중 29건이
자금거래로 잡혔다** — 대부분 급여였다. 자금거래는 손익에서 빠지므로 급여 3천만원이 조용히
사라질 수 있었다.

→ 사람 이름으로는 **절대** 판정하지 않게 고쳤다. 인정하는 근거는 두 가지뿐:
- 상대 **계좌번호** 뒷 4자리가 우리 마스터에 있다 (토스가 이걸 준다 — 가장 확실)
- 상대 이름이 우리 **법인명**이다 ((주)네안데르 등)

수정 후 토스 자금거래 29건 → 0건, 카카오 4건 → 0건. 신한은행은 법인명 7건만 남았다.

**② 거래유형과 계정의 모순**
은행은 입출금 **방향**만 알고 성격은 모른다. 이력이 `투자` 라는 거래처를 자금거래로 분류해
왔는데 어댑터는 지출로 판정 → `지출 + 계정 계좌간이동` 이라는 모순이 생겼다. 리포트의
`inBasis` 는 거래유형으로 자금거래를 빼기 때문에 비손익 거래가 손익에 잡힌다.

→ `classify.ts` 가 계정 마스터를 참조해 검증한다:
- 계정이 자금거래·카드대금결제 계열이면 **거래유형을 고쳐 제안**한다 (이력이 더 정확하다)
- 수입 ↔ 지출이 어긋나면 계정을 버리고 `needs_review` 로 넘긴다 (부호가 뒤집히므로)
- 예외 하나: `카드대금결제` 유형 + `재무비용>금융비용>카드대금결제` 계정은 **의도된** 조합
  (계정은 재무비용 밑이라 지출이지만 유형은 별도) — 기존 장부의 확립된 관행

실측 교정: 카카오 4건(`김주연` 지출→자금거래, `투자` 지출→자금거래), 토스 1건.

**③ 국민 법인카드의 달러 금액**
`$8.85` 를 그냥 읽으면 8원짜리 지출이 된다. 통화를 감지해 원문을 보존하고, 환율을 받기 전에는
**적재하지 않고 오류로 돌린다** (0원으로 넣으면 조용히 사라진다). 신한은 원화 환산을 주므로
환율이 필요 없다.

**④ 페이히어 POS 는 적재하지 않는다 — 이중 계상**
현재 장부는 매장 매출을 **카드사 정산 입금**으로 잡고 있다 (2026-07 와우판매 105건이
삼성·KB·NH·SHC·하나 등 17종 카드사 입금). POS 를 함께 넣으면 매출이 두 번 잡힌다.

→ POS 는 **대사(對査)** 전용으로 만들었다. POS 실매출 vs 장부 매출을 맞춰 보고 그 차이를
보여준다 — 차이가 카드 수수료와 미정산분이다. 실측(아이디, 7/24~8/24):
POS 4,376,200(164건) vs 장부 2,678,727(7건) → 차이 △1,697,473 (38.8%).

(POS 를 매출의 정본으로 바꾸고 정산 입금을 자금거래로 돌리는 방법도 있다. 회계적으로는 그게
더 정확하지만 과거 장부 전체를 다시 정의해야 하므로, 결정하기 전까지는 대사만 한다.)

### 2.5 검증 (5·6월 실제 파일 9개)

```
279행 파싱 → 중복 199건 제외 → 새 거래 80건
자동분류: 확정 8 · 제안 36 · 검토필요 36 (자동확정 10%)
유형별: 지출 63건 22,411,498 | 카드대금결제 7건 6,690,949 | 수입 10건 7,104,663
거래유형·계정 모순 0건 · 날짜 형식 오류 0건 · 금액 0/NaN 0건
```

자동확정 10% 가 낮아 보이지만, 5·6월분은 이미 적재돼 있어 남은 80건이 **전부 신규 거래처**다.
`장부자동화 조사보고` 시트의 실측(신규 거래처 22%, 모호 19%)과 방향이 같다.

---

## 3. AI 분류 도우미

### 왜 필요한가

규칙(`classify.ts`)은 거래처가 **정확히** 일치할 때만 맞힌다. 실측:

| 거래처 | 정확 일치 | 유사 매칭 |
|---|---:|---|
| `FACEBK *NEWRANDOM99` | **0건** | 4건 → `FACEBK *8CQJANVM62` → 온라인광고비 |
| `CANVA* I09999-1234567` | **0건** | 3건 → `CANVA* I04901-26328047` → 구독서비스비 |

뒤의 난수만 다른 같은 거래처를 규칙은 절대 못 잡는다. `한국전력공사`→전기수도통신비,
`카카오택시-서울34사3106`→교통비 같은 판단도 언어 이해가 필요하다. 그 지점에서만 모델을 쓴다.

### 구현 — **OpenRouter 경유**

이미 OpenRouter 를 쓰고 있고 그 비용이 장부에 잡혀 있다(2026-07 툴구독비 646,176원 + 대납
513,975원). 키를 하나 더 만들지 않고 기존 창구를 쓰면 사용량이 한 곳에 모여 `OpenRouter배분`
시트처럼 프로젝트별 배분도 그대로 이어진다.

- `lib/neander/finance/server/ai-classify.ts` — 프롬프트 구성 + 호출 + 검증
- `app/api/neander/finance/ai/suggest/route.neander.ts` — 클라이언트는 **거래 id 만** 보낸다.
  거래 내용·계정 마스터·이력은 서버가 Firestore 에서 읽는다 (무엇이 모델에 갔는지 서버가 알아야 한다).
- 검토 대기함에 「AI 추천」 버튼. 결과는 각 행에 얹히고 **저장되지 않는다.**

OpenRouter 는 OpenAI 호환 `chat/completions` 라 Anthropic SDK 대신 `fetch` 로 직접 부른다 —
엔드포인트 하나뿐이라 의존성을 더할 이유가 없다. 대신 Anthropic 특유의 두 가지는 챙긴다:

| 항목 | 설정 |
|---|---|
| 모델 | `anthropic/claude-opus-5` (`OPENROUTER_MODEL` 로 변경 가능). 1M 컨텍스트, 구조화 출력 지원, 단가는 직접 호출과 동일($5/$25 per 1M) |
| 구조화 출력 | `response_format: {type:"json_schema", json_schema:{name, strict:true, schema}}` |
| 제공자 고정 | `provider: {require_parameters: true}` — 구조화 출력을 지원하지 않는 제공자로 새면 스키마가 무시돼 파싱이 깨진다 |
| 프롬프트 캐시 | system 블록에 `cache_control: {type:"ephemeral"}`. Anthropic 모델은 명시해야 걸린다. 캐시 읽기 $0.5/1M (입력의 0.1배) |
| 추론 강도 | `reasoning_effort: "high"` |
| 사용량 | `usage.prompt_tokens_details.cached_tokens` · `cache_write_tokens` · `cost` — 화면에 토큰과 **달러 비용**을 함께 띄운다 |

프롬프트에 넣는 재료: 거래 정보 + **유사 과거 확정 거래 4건**(부분 일치로 찾은 것) — 정확 일치는
규칙이 이미 시도했으므로 여기서는 부분 일치가 값이다. system ≈4.2K 토큰(캐시됨), 5건 기준 user ≈545 토큰.

### 안전장치 세 가지

1. **절대 `confirmed` 로 만들지 않는다.** 확신 70% 이상은 `suggested`, 그 아래는 `needs_review`.
   사람이 검토 대기함에서 승인한다.
2. **계정 마스터에 없는 계정은 버린다.** 모델이 그럴듯한 이름을 지어내면(`운영비>일반운영비>SaaS비용`)
   조인이 깨진다. 317개 잎 계정과 정확히 일치하지 않으면 채택하지 않고, **버린 건수를 화면에 알린다.**
3. **거래유형 정합성** — `지출` 거래에 `[수입]` 계정을 제안하면 폐기.

### 실제 호출 검증 (2026-08-24)

키를 넣고 예시 파일에서 나온 **규칙이 못 맞힌 실제 거래**로 세 번 호출했다.

**호출 1 — FACEBK 5건** (규칙이 절대 못 맞히는 난수 거래처)
전부 `마케팅비>프로모션이벤트>온라인광고비` 확신 93~95%. 14.6초, $0.09028.
캐시 쓰기 10,247 토큰(첫 호출이라 미스).

**호출 2 — 같은 계열 15건** — 캐시 **읽기 10,247 / 쓰기 0**. 22.8초, $0.07719.
건수가 3배인데 비용은 더 쌌다. 프롬프트 캐시가 실제로 걸린다는 확인.

**호출 3 — 서로 다른 거래처 16건** (거래처 뿌리별로 하나씩 골라 다양성 확보)
23.2초, $0.07230, 폐기 0건. 결과:

| 거래처 | 추천 | 확신 |
|---|---|---:|
| 2604국민건강 / 고용보험 / 산재보험 / 국민연금 | 인건비>사대보험>각각 | 96% |
| 한국전력공사 | 운영비>홍대공용운영비>전기수도통신비 | 90% |
| KLINGAI.COM · SGT*justdonecom | 운영비>일반운영비>구독서비스비 | 88·85% |
| 유선화 · 이동주 | 인건비>급여>임원급여 | 82% |
| 유재영 | 인건비>급여>임원급여 | 70% *"프리랜서비 이력도 있어 확인 권장"* |
| (주)어반플레이 · 성오푸드 | 인건비>복리후생비>일반식대 | 85·75% |
| 봄이보리밥 · 칼디커피 | 영업비>대외관계거래관리비>B2B영업미팅비 | 70% *"직원 식대 가능성도 있음"* |
| 케이-모터스 | 운영비>차량관리비>차량유지비 | 60% *"금액이 122만원으로 커 정비비 가능성"* |

**확신도 보정이 실제로 작동한다** — 확실한 건 96%, 애매한 건 60~70% 로 낮추면서 왜 애매한지까지
밝힌다. 그 이유가 그대로 검토 대기함의 근거 문장으로 들어간다.

**비용**: 16건에 $0.0723 = 건당 약 $0.0045(6원). 한 달 검토필요가 36건이면 **월 200원 남짓**이다.

**실제 호출로 잡은 버그**: `X-Title` 헤더에 한글(`NEANDER ERP 재무 분류`)을 넣어 fetch 가
`Cannot convert argument to a ByteString` 로 죽었다. HTTP 헤더는 latin-1 만 담을 수 있다 →
ASCII 로 고쳤다. 정적 검증으로는 못 잡는 종류라, 실제 호출을 한 것이 값을 했다.

### 남은 일

`OPENROUTER_API_KEY` 를 **Vercel 환경변수(배포)** 에도 넣어야 배포본에서 동작한다
(로컬 `.env.local` 은 설정 완료). 모델을 바꾸려면 `OPENROUTER_MODEL` 도 함께.

---

## 4. 다음에 할 만한 것

- **AI 월 마감 브리핑** — 이상 징후(급증 계정·미분류 잔량·예산 초과·구독 중복)를 한 문단으로.
  재료는 이미 다 있다(리포트 엔진·예산·구독 경고).
- **거래처 정규화 학습** — AI 가 `FACEBK *xxx` 계열을 하나의 거래처로 묶어주면 그걸 거래처 규칙에
  자동 등록해 다음 달부터는 규칙만으로 맞힌다. AI 호출이 줄어든다.
- **네이버 스마트스토어** 어댑터 — 예약과 별개인 온라인 판매 채널.
- **`Npay정산` 자동 귀속** — 지금은 아이디판매로 잡히는데, 네이버 예약 파일과 대사해서 실제
  귀속(매장 체험 vs 온라인)을 확정하면 규칙으로 굳힐 수 있다.
- 팝빌·페이히어 API (계약 결정 후) — 어댑터 체계가 있으므로 수집 경로만 바꿔 끼우면 된다.
